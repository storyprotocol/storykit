import { render } from "@testing-library/react"

import StakeButton from "../StakeButton"

describe("StakeButton", () => {
  test("renders the StakeButton component", () => {
    render(<StakeButton>hi</StakeButton>)
  })
})
